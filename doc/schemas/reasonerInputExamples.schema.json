{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Scenario Reasoner input for scenarios.examples method",
  "type": "object",
  "properties": {
    "method": {
      "description": "The method of the scenario reasoner being called",
      "type": "string",
      "enum": [ "scenarios.examples" ]
    },
    "params": {
      "$ref": "D:/Communicate/Schema/reasonerInputId.schema.json" // URI to the referred JSON schema, for now a local path
    }
  },
  "additionalProperties": false,
  "required": [ "method", "params" ]
}