{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Scenario Reasoner input for scenarios.allfirsts method",
  "type": "object",
  "properties": {
    "method": {
      "description": "The method of the scenario reasoner being called",
      "type": "string",
      "enum": ["scenarios.allfirsts"]
    },
    "params": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": {
        "$ref": "D:/Communicate/Schema/state.schema.json#" // URI to the referred JSON schema, for now a local path
      }
    },
    "id": { "description": "A value identifying this request" }
  },
  "additionalProperties": false,
  "required": ["method", "params"]
}